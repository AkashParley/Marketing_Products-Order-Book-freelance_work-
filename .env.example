# Copy this file to .env and fill in your Supabase project credentials.
# Without these set, the app runs fully offline using localStorage
# (seeded with the Geeta Ram & Sons demo order) — useful for trying the
# app before setting up a database.

